function createTable() {
    return new Promise(function (resolve, reject) {
        if (!a || !b) return reject("Invalid input!")
        setTimeout(function () {
            resolve()
        }, 1000);
    });
}
